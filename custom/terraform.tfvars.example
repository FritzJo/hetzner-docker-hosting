hcloud_token = ""
hcloud_floating_ip = ""
hcloud_server_type = "cx11" # Change to "cax11" for ARM VMs

# Restrict SSH access to specific IP ranges (recommended).
# If unset, SSH is open to 0.0.0.0/0 (any IP).
# Example: ssh_source_ips = ["203.0.113.0/24", "198.51.100.0/24"]
# ssh_source_ips = []